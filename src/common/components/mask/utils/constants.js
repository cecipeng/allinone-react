export const actionTypes = {
  SHOW_MASK: 'SHOW_MASK', // 显示遮罩层，通用于弹窗的显示
  HIDDEN_MASK: 'HIDDEN_MASK', // 隐藏遮罩层，通用于弹窗的显示
  UPDATE_MASK_ZIndex: 'UPDATE_MASK_ZIndex' // 更新遮罩层zindex，用于特殊层级时重新设置遮罩的zindex
}
